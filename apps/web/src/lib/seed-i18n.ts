/**
 * seed-i18n.ts — Arquivo derivado (D-183)
 * Gerado por scripts/gen-seed-i18n.ts — NÃO editar manualmente.
 * Fornece titleLocalized{pt,en,es} e genreSlugs por ID/slug de entidade do mock.
 */

export const SEED_I18N: Record<string, {
  titleLocalized: { pt: string; en: string; es: string };
  genreSlugs: string[];
}> = {
  // Exemplo (preenchimento incremental):
  // "1275779": { titleLocalized: { pt: "O Resgate do Soldado Ryan", en: "Saving Private Ryan", es: "Rescatando al Soldado Ryan" }, genreSlugs: ["drama","guerra"] },
};
