CREATE TABLE "uso_diario" ("id" UUID NOT NULL, "usuario_id" UUID NOT NULL, "recurso" VARCHAR(32) NOT NULL, "dia" VARCHAR(10) NOT NULL, "count" INTEGER NOT NULL DEFAULT 0, CONSTRAINT "uso_diario_pkey" PRIMARY KEY ("id"));CREATE UNIQUE INDEX "uso_diario_usuario_id_recurso_dia_key" ON "uso_diario"("usuario_id", "recurso", "dia");CREATE INDEX "uso_diario_usuario_id_idx" ON "uso_diario"("usuario_id");ALTER TABLE "uso_diario" ADD CONSTRAINT "uso_diario_usuario_id_fkey" FOREIGN KEY ("usuario_id") REFERENCES "usuario"("id") ON DELETE CASCADE ON UPDATE CASCADE;