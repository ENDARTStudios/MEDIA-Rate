# Dependencias
node_modules/
.pnp
.pnp.js

# Builds
dist/
build/
out/
.next/
coverage/
*.tsbuildinfo
coverage-out.txt
cov*.txt

# Ambiente e segredos (NUNCA commitar)
.env
.env.*
!.env.example
.env.local
.kilo/exchange_log.jsonl
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Cache
.eslintcache
.cache/
.parcel-cache/
.turbo/

# Testes
.nyc_output/
.stryker-tmp/

# Skills locais (do ambiente, nao do projeto)
skills/

# Uploads de usuario (quando Fase 6 confirmar upload)
uploads/

# Diretorios de trabalho do agente
agent-ctx/

# T278: artefatos acidentais de ferramentas IA / curls — nunca commitar
mnt/
.agent-log
-w
agent-logs/
outputs/
.claude/

.vercel
.env*
# Runtime checkout do agente (nunca faz parte do repositório do produto).
deepseek-harness/
# Exceção: .env.example é documentação de configuração (apenas placeholders,
# nenhum valor real) — entregável do T202 para configurar ambientes do zero.
!**/.env.example
# T206: caixa de entrada do email mockado em dev (contém tokens de reset
# brutos — NUNCA commitar; o app log só recebe hash truncado).
dev-mailbox.log
apps/web/src/components/hero-icons/*.png

# e2e test password (local, nunca commitado)
.e2e-pass
